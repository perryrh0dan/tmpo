// test template for errors
use log;
use std::process::exit;

use crate::action;
use crate::cli::input::select;
use crate::config::Config;
use crate::out;
use clap::ArgMatches;

pub fn test(config: &Config, args: &ArgMatches) {

}
