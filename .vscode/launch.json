{
  "version": "0.2.0",
  "configurations": [
      {
          "name": "(Windows) Launch",
          "type": "cppvsdbg",
          "request": "launch",
          "program": "${workspaceRoot}/target/debug/charon.exe",
          "args": ["init", "2fa", "typescript", "."],
          // "args": ["list"],
          "stopAtEntry": false,
          "cwd": "${workspaceRoot}",
          "environment": [],
          "externalConsole": true
      },
      {
          "name": "(OSX) Launch",
          "type": "lldb",
          "request": "launch",
          "program": "${workspaceRoot}/target/debug/charon",
          "args": ["init", "2fa", "-t", "golang"],
          // "args": ["list"],
          "cwd": "${workspaceRoot}",
      }
  ]
}